package me.paranid5.resources

import androidx.compose.ui.graphics.Color

object Colors {
    val primaryColor = Color(140, 25, 225)
    val backgroundColor = Color(30, 30, 30)
    val textColor = Color(0, 0, 0)
}